{
    "name": "legion-token-tracker-subgraph",
    "version": "0.1.0",
    "description": "The Graph subgraph for Legion.cc sale contract",
    "repository": "https://github.com/your-username/legion-token-tracker",
    "license": "MIT",
    "scripts": {
        "codegen": "graph codegen",
        "build": "graph build",
        "deploy": "graph deploy --node https://api.thegraph.com/deploy/ legion-token-tracker",
        "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 legion-token-tracker",
        "create-local": "graph create --node http://localhost:8020/ legion-token-tracker",
        "remove-local": "graph remove --node http://localhost:8020/ legion-token-tracker"
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "0.56.0",
        "@graphprotocol/graph-ts": "0.31.0"
    },
    "devDependencies": {
        "matchstick-as": "0.5.0"
    }
}